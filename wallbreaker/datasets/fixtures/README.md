# Offline dataset fixtures

Tracked sample corpora so FRR batteries work without network
and without relying on the gitignored top-level `library/` directory.

Full remote corpora still refresh into `library/` via `wallbreaker datasets refresh`.
